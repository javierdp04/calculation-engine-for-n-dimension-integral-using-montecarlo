import { MonteCarloMotor } from "../src/monteCarlo";
import { Scope } from "../src/models/models";

describe('MonteCarloMotor', () => {
    let motor : MonteCarloMotor

    beforeEach(() => {
        motor = new MonteCarloMotor();
    })

    it("Test for N = 1", () => {

    })

    it("Test for N = 2", () => {

    })

    it("Test for N = 10", () => {

    })

})